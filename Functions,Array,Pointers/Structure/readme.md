🟢 Basic Level

 1. Define a structure Student with members:

        roll number
        name
        marks

2. Write a program to input and display details of one student.
3. Write a program to store and display details of 5 employees using structures.
4. Create a structure Book having:

        book_id
        title
        price
        
5. Initialize the structure and print the details.
6. What is the difference between structure and union? Explain with example.

🟡 Intermediate Level

7. Write a program to calculate the average marks of a student using a structure.
8. Create a structure Date (dd, mm, yyyy).
9. Write a program to compare two dates.

10. Write a program to pass a structure to a function and display student details.

11. Write a program using array of structures to store and print details of n students.

12. Create a structure Employee and sort employees by salary.

🔵 Advanced Level

13. Write a program to find the student with highest marks using structures.
14. Write a program using nested structures for:
        Student
        Address (city, pincode)

15. Write a program using structure pointer (->) to access members.
16. Write a menu-driven program using structure to:
        Add record
        Display record
        Search record by ID


🟢 Level 1 – Easy

17.  Sum of Array Using Function
    
        Write a C program that:
        Takes 5 integers in an array using a loop
        Passes the array to a function
        The function returns the sum of all elements

18. Count Even and Odd Numbers

        Write a program that:

        Reads n elements into an array

        Uses a function to count how many numbers are even and odd

19. Find Maximum and Minimum

        Write a program that:

        Stores 10 numbers in an array

        Uses a function to find the maximum and minimum values

19. Reverse an Array

        Write a program that:
        Reads n elements in an array
        Uses a function to reverse the array
        Prints the reversed array using a loop

20. Search an Element

        Write a program that:

        Takes an array of n elements

        Uses a function to search for a given element

        Returns its position if found, otherwise -1

21.  Count Frequency of Each Element

        Write a program that:
        Stores n integers in an array
        Uses a function to print thefrequency of each element

22. Sort Array Using Function

        Write a program that:
        Reads n elements into an array
        Uses a function to sort the array in ascending order
        Displays the sorted array

23. Merge Two Arrays

        Write a program that:
        Reads two arrays
        Uses a function to merge them into a third array
        Prints the merged array

24. Check Palindrome Array

        Write a program that:
        Reads an array
        Uses a function to check whether the array is a palindrome

25. Pass Array to Function and Modify It

   \     Write a program that:
        Stores numbers in an array
        Uses a function to square each element of the array
        Prints the modified array

    